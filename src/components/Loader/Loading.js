import React from 'react';
import Loading from 'react-loading-components';
 
const Load = (props) => (
  <Loading style={{margin: '400px auto'}} type='three_dots' width={100} height={100} fill='#444545' />
);
 
export default Load;